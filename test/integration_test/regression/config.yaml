trainer:
  num_epochs: 10
  task: regression

metrics:
  - rmse
  - mae

log:
  verbose: 1

early_stop:
  metric: val_loss
  patience: 3
  min_delta: 0.0001

checkpoint:
  metric: val_loss
  min_delta: 0.0001
